# =========================================
# FILE: backend/ai/search_ai.py
# =========================================

class SearchAI:

    def search_topic(self, topic):

        return [
            f"{topic} result 1",
            f"{topic} result 2",
            f"{topic} result 3"
        ]
